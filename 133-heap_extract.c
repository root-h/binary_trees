#include "binary_trees.h"

/**
 * heap_extract - extracts root node of Max Binary Heap
 * root node must be freed and replaced withh the last level-order node
 * once replaced, heap must be rebuilt if necessary
 * @root: double pointer to root node of heap
 * Return: value stored in root node, otherwise 0 on failure
 */

int heap_extract(heap_t **root)
{
	return;
}