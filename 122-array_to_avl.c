#include "binary_trees.h"

/**
 * array_to_avl - builds AVL tree from array
 * @array: array to convert
 * @size: length of array / number of elements in array
 * Return: pointer to root node of created AVL tree, otherwise NULL if failed
 */

avl_t *array_to_avl(int *array, size_t size)
{
	return;
}